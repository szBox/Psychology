import Vue from 'vue'
import Vuex from 'vuex'
import * as types from '../vuex/type';
Vue.use(Vuex)

const user = {
 	base:[],
  mutations: {
   
  },
  actions: {
   
  }
};
const store = new Vuex.Store({
	user
	
})

export default store